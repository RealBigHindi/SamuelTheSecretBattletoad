using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// Welcome to CSharp


/*
 * This is a class.
 * Classes are holding variables and methods.
 **/
public class A_Basics
{
    // 1. This are variables
    int integer_1;
    int integer_2 = 1;
    // it consists of data type [int], a name [integer_1] and an optional value [= 1]

    // 2. this is a method
    void myMethod(int iAmOptional)
    {
        integer_1 = integer_2;
    }
    // it consists of the return type [void] (which means 'no returned value'), the name [myMethod], optional parameters [(int iAmOptional)] and curly braces {which cotain the code}

    /*
     * To run CSharp code you would usually write a 
     * */
}
